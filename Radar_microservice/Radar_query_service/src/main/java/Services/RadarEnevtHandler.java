package Services;
/*
import Entities.Radar;
import Repositories.RadarRepository;
import events.RadarCommandCreated;
import lombok.AllArgsConstructor;
import lombok.extern.slf4j.Slf4j;
import org.axonframework.eventhandling.EventHandler;
import org.springframework.stereotype.Service;

@Service
@Slf4j
@AllArgsConstructor
public class RadarEventHandler {
    private RadarRepository radarRepository;
    @EventHandler
  public void on(RadarCommandCreatedevent){
        log.info("*************RadarCreatedEvent received!*************");
        Radar radar=new Radar();
        radar.setId(event.getId());

        radarRepository.save(radar);
 }  */
